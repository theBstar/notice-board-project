import React from 'react';
import './subscribe.css';

export default function(props) {
    return (
        <button className="subscribe">
            Subscribe  <i className="fa fa-bell fa-lg"></i>
        </button>
    );
}